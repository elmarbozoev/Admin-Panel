@model Achievement

<h1>@Model.Name</h1>

<p>
    @foreach (var mediaFile in Model.MediaFiles)
    {
        @if(mediaFile.Path.EndsWith(".mp4"))
        {
            <video height="100px" controls>
                <source src="@mediaFile.Path" type="video/mp4">
                @mediaFile.Path
            </video>
        }
        else
        {
            <img height="100px" src="@mediaFile.Path" alt="@mediaFile.Path">
        }
    }
</p>

<p>@Model.Description</p>

<form asp-action="Index" asp-controller="Achievement" method="get">
    <input type="submit" value="Back" />
</form>